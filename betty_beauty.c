#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char *message = "I am an ALX software engineering student doing hard things.";

	printf("%s\n", message);

	return (0);
}
